# Full Stack Project: Angular + ASP.NET Core Web API

This project includes:

- **Frontend**: Angular (inside `/ProductListInterface`)
- **Backend**: ASP.NET Core Web API (inside `/ProductListApp`)

## How to Run

### Backend (ASP.NET Core)
```bash
cd ProductListApp
dotnet restore
dotnet run
